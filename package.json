{
  "name": "chatbot-nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "8.11.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "(cd client && yarn add) && (yarn add)",
    "server": "cd server && nodemon index.js",
    "client": "cd client && yarn start",
    "heroku-postbuild":
      "cd client && yarn --production=false && yarn run build",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apiai": "^4.0.3",
    "body-parser": "^1.18.2",
    "concurrently": "^3.5.1",
    "express": "^4.16.3",
    "nodemon": "^1.17.3",
    "request": "^2.85.0"
  }
}
